apiVersion: v1
kind: PersistentVolumeClaim
metadata: 
  name: telegraf-pvc
  namespace: telegraf
spec: 
  accessModes: 
    - ReadWriteOnce
  resources: 
    requests: 
      storage: 1Gi
  storageClassName: metrics-nfs-storage

