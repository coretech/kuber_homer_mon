apiVersion: v1
kind: PersistentVolumeClaim
metadata: 
  name: influx-pvc
  namespace: influx
spec: 
  accessModes: 
    - ReadWriteOnce
  resources: 
    requests: 
      storage: 2Gi
  storageClassName: metrics-nfs-storage

